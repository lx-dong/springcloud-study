eureka:
  client:
    serviceUrl:
      defaultZone: http://peer1:8761/eureka, http://peer2:8762/eureka

server:
  port: 8765

spring:
  application:
    name: service-feign

feign:
  hystrix:
    enabled: true